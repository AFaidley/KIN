const User = require('./User');
const Event = require('./Event');
const Group = require('./Group');
const Post = require('./Post');
const Comment = require('./Comment');

module.exports = { User, Event, Group, Post, Comment };
